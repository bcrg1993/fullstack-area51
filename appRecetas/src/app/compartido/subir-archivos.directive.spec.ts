import { SubirArchivosDirective } from './subir-archivos.directive';

describe('SubirArchivosDirective', () => {
  it('should create an instance', () => {
    const directive = new SubirArchivosDirective();
    expect(directive).toBeTruthy();
  });
});
